The included project files (sqlplusint/*.bp? and examples/*.bp?) were
made for Borland C++ Builder 6.0 by Totte Karlsson.  Totte writes:

	"Any Borland user has to define macros (under IDE user variables)
	for the "main" sql installation directory, e.g. MySQL = c:\mysql
	and also change where the .lib file is being created.

	It is not very well tested however..."
